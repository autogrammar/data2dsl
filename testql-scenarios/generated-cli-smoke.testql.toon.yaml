# SCENARIO: CLI smoke tests for data2dsl
# TYPE: cli
# GENERATED: true

CONFIG[2]{key, value}:
  cli_command, data2dsl
  timeout_ms, 10000

LOG[3]{message}:
  "Test CLI help command"
  "Test CLI version command"
  "Test CLI doctor subcommand"
