# Ticket 005: Generate project ticket README

- **ID**: ticket-005
- **Owner**: unresolved:human
- **Status**: DONE
- **Workflow state**: DONE
- **Created**: 2026-08-17

## Goal and scope

Use the repository-owned `project/readme.sh` generator to create
`project/README.md` as a deterministic ticket index. The generator's safety
boundary must remain intact: it may write only below `project/` and must not
replace the root product `README.md`.

## Acceptance criteria

- [x] AC-01: `project/README.md` is generated by `project/readme.sh` through
  its supported `T2C_TICKET_INDEX_FILE` input.
- [x] AC-02: The generated table contains every tracked ticket from 001
  through 005 with valid repository-relative links.
- [x] AC-03: Running the generator twice produces no second diff.
- [x] AC-04: The root `README.md` remains byte-for-byte unchanged.
- [x] AC-05: The governance gate passes.

## Result

`project/README.md` was generated directly by the repository-owned script. It
contains the complete tracked ticket index through ticket-005. The canonical
root product overview was intentionally preserved because the generator
rejects output outside `project/`.

Publication completed through protected PR #6. Validator App approved exact
head `2d0d5230c0846bcef9cad14678a8b13bec799343`, merged it as
`adc3015e284a3b8970bfb36f6681205ec16e9f79`, and deleted the ticket branch.

## Participants

- Human participant: unresolved; no user-* file was created by this script.
- Agent participant: [ai-codex.md](ai-codex.md)
