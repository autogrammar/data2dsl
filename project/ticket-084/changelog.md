# Ticket Changelog (ticket-084)

## [0.1.0] - 2026-08-31

- Initial governance scaffold created.
- Fix actor matching regex in `WorkSummaryMarkdownAdapter`.
- Fix OQL buses attribute resolution in `OqlTelemetryAdapter`.
- Fix exact key matching in `SUMDAdapter`.
- Fix Code2Schema `entities` keyword and numeric 0 coalescing in `data2dsl_skill.py`.
- Sanitize evidence IDs to replace `/` with `:`.
